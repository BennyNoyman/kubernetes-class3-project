apiVersion: v1
kind: Service
metadata:
  name: banner-server-service
  namespace: banner
spec:
  selector:
    app: demo-store-server
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
